# macOS
.DS_Store

# Julia
*.cov
*.jl.cov
*.jl.*.cov
*.jl.mem
deps/build.log

# LaTeX intermediates (in case any examples include them)
*.aux
*.log
*.synctex.gz
*.toc
*.bbl
*.blg
*.out

# Override the global ~/.gitignore_global rule that excludes *.pdf —
# slide deliverables in slides/ should be committed.
!slides/*.pdf
